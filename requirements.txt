# Interfaz gráfica
customtkinter>=5.2

# Manejo de imágenes (logo en la pantalla de login). customtkinter ya
# depende de esto internamente, pero se deja explícito porque
# ui/login_window.py también la usa directamente.
Pillow>=10.0

# Login con Microsoft (Entra ID / Azure AD), device code flow
msal>=1.30

# Opcional: si está instalado, la prueba de conexión a SQL Server usa una
# conexión real; si no está instalado, se hace un fallback automático a una
# prueba de conectividad de red (socket). La app funciona en ambos casos.
# pyodbc

# Verificación de firma digital de actualizaciones (Ed25519).
# Ver core/update_security.py. Sin esto, el sistema de updates degrada
# a exigir checksum SHA-256 como mínimo (nunca instala sin verificar).
cryptography>=42.0

# Almacenamiento seguro de credenciales (API keys, contraseña de BD)
# en el llavero nativo del SO: Windows Credential Manager / macOS
# Keychain / Secret Service (Linux), en vez de texto plano en disco.
keyring>=25.0

# Calidad / desarrollo
pytest>=8.0
